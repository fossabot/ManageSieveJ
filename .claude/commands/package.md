---
description: Package the project as a JAR file
---

Package the ManageSieveJ project into a JAR file. Run `mvn clean package` to compile, test, and create the JAR artifact.

The JAR file will be created in the target/ directory. Report the location and size of the created artifact.
