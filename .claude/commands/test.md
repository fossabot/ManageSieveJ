---
description: Run the test suite with Maven
---

Run the test suite for ManageSieveJ using Maven. Execute `mvn test` to run all TestNG tests.

If any tests fail, analyze the failures and fix them. Report test coverage statistics if available.
